{{#each books as |book|}}
  {{log book}}
  <!-- {{> partial}}
  Don't uncomment yet -->

  <h1 data-id="{{book.id}}"> <small>Title: {{book.title}}</small></h1>
  <div>
    {{#if book.author}}
      {{book.author}}
    {{else}}
      No author provided.
    {{/if}}
  </div>

{{/each}}
